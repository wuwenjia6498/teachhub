# AIHUBMIX 平台 API Key（用于上传时调用 Gemini 生成文档摘要）
# 前往 https://aihubmix.com 注册并获取 Key，填入下方
AIHUBMIX_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 管理后台密码
ADMIN_PASSWORD=your_password_here

# Upstash Redis 连接信息（替代原 Vercel KV）
# 在 Vercel 控制台 Storage → Create Database → Upstash → Redis 创建后，
# 进入数据库 → .env.local 一键复制以下两项
UPSTASH_REDIS_REST_URL=https://xxxxxxxx.upstash.io
UPSTASH_REDIS_REST_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Vercel Blob 读写 Token（用于 docx 上传时外置图片 & 历史数据迁移）
# 在 Vercel 控制台 Storage → Create Database → Blob 创建后，
# 进入数据库 → .env.local 一键复制该变量
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 关闭防复制保护（右键 / 选中文字 / Ctrl+C / Ctrl+A）
# 调试或需要验证图片 URL 时设 1，生产环境请留空或设 0 以保持拦截。
# 变量需带 NEXT_PUBLIC_ 前缀才能被客户端组件读取。
NEXT_PUBLIC_DISABLE_COPY_GUARD=0
